[package]
name = "sweefi-escrow"
version = "0.1.0"
description = "SweeFi Escrow — time-locked vault with arbiter dispute resolution"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]
name = "sweefi_escrow"

[features]
default = []
cpi = ["no-entrypoint"]
no-entrypoint = []
no-idl = []
no-log-ix-name = []
idl-build = ["anchor-lang/idl-build", "anchor-spl/idl-build"]

[dependencies]
anchor-lang = { workspace = true }
anchor-spl = { workspace = true }
borsh = { version = "1", features = ["derive"] }
