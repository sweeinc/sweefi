{
  "name": "@sweefi/facilitator-demo",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Demo: facilitator-as-a-service — three-party s402 settlement with metering",
  "license": "Apache-2.0",
  "scripts": {
    "demo": "node --env-file=.env --import tsx/esm demo.ts",
    "server": "node --env-file=.env --import tsx/esm server-a.ts",
    "agent": "node --env-file=.env --import tsx/esm agent.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@hono/node-server": "^1.14.0",
    "@mysten/sui": "^2.6.0",
    "@sweefi/server": "workspace:*",
    "@sweefi/sui": "workspace:*",
    "hono": "^4.7.0",
    "s402": "^0.2.0"
  },
  "devDependencies": {
    "tsx": "^4.7.0",
    "typescript": "^5.5.0"
  }
}
